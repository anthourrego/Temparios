import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.prosof.ProcessApp',
  appName: 'ProcessApp',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
